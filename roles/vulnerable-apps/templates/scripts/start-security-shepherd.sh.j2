#!/bin/sh

docker-compose -f {{shepherd_compose_file}} up -d
echo "Visit http://localhost:{{shepherd_host_port_http}} (HTTP) or http://localhost:{{shepherd_host_port_https}} (HTTPS, self-signed certificate)"
echo "Visit https://github.com/OWASP/SecurityShepherd for the docs."